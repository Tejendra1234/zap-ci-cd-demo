name: ZAP Baseline

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  zap_scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run ZAP Baseline Scan
        uses: zaproxy/action-baseline@v0.13.0
        with:
          target: "http://example.com"
          cmd_options: "-a"
          allow_issue_writing: false
          # 🚫 remove artifact_name completely

      - name: Upload ZAP Reports
        uses: actions/upload-artifact@v4
        with:
          name: zap-reports   # ✅ simple valid name
          path: |
            report_html.html
            report_md.md
            report_json.json
